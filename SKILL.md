---
name: awesome-literature-review
description: 中文学术文献综述全流程生成。分四部分(研究背景与目的、文献梳理与分析、研究现状总结与评述、参考文献),开场先对齐题目、文献来源、输出格式与引用规范,搜文献前再确认文献规模、年份跨度、来源门槛与语种配比,清单经核验后一次性写完整篇再输出;文献可用户已有材料抽取、自己粘贴或 AI 联网搜索,写入正文的每一篇都要有真实出处,正文里的方法名与数据只能来自实际读到的摘要或全文,读不到就只写到主题层面,既防编造引用也防编造结论;评述部分要求指出结论分歧、评估方法学局限、把研究空白落到具体对象与方法上;参考文献默认国标 GB/T 7714-2015;支持对话/Markdown/Word/LaTeX 输出,可选调用 Image2 生成文献分类框架图。本 skill 面向叙述性文献综述,不做元分析和统计合并,不代做 PRISMA 系统综述的筛选与偏倚评价。触发:文献综述、写综述、literature review、帮我写文献综述、国内外研究现状。
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Agent
  - AskUserQuestion
  - WebSearch
  - WebFetch
---

# 文献综述生成 Skill

按四部分推进一篇文献综述:研究背景与目的 → 文献梳理与分析 → 研究现状总结与评述 → 参考文献。
核心是**前期对齐充分、文献真实可核验、结论不凭空补**:开场把题目、文献来源、输出格式、引用规范一次性问清,
搜文献前再把检索口径定下来,文献清单经用户核验,然后一口气写完整篇初稿再交付,不再一段段停下来确认。

## 触发条件

- 用户要写文献综述 / 国内外研究现状
- 关键词:文献综述、写综述、literature review、帮我写文献综述、国内外研究现状

## 适用边界

- **做**:叙述性文献综述、学位论文的国内外研究现状、期刊论文里的研究现状部分。
- **不做**:元分析与统计合并(效应量、异质性检验、森林图),也不代替系统综述的文献筛选和偏倚风险评价。
- 用户在做系统综述时,本 skill 只负责把**用户自己确认过的筛选数字**画成 PRISMA 流程图,
  不替用户判断纳入排除、不推算数字。数字不齐就留 `n = ?`,并在交付说明里列出待补项。

## 四条铁律

1. **文献必须真**:写入正文的每一篇文献都要有可核验来源。用户自己粘的照用;AI 搜的必须带出处链接、
   先给用户核对,禁止凭记忆编造标题、作者、年份。
2. **结论必须有依据**:正文里写某篇文献"做了什么、得出什么",只能依据实际读到的原文——用户提供的摘要、
   WebFetch 打开的摘要或全文页面。手里只有标题和年份的,就只写到主题层面(讨论了什么问题、属于哪条技术路线),
   **禁止补出原文没有的方法名、模型名、数据集、准确率和百分比**。参考文献列表全真、正文转述半编,
   比整条假引用更难查、危害更大。清单里每篇文献的证据等级在 Phase 1 标出来,写作时按等级决定能写多细。
3. **前期对齐、一次成稿**:干预集中在动笔之前——开场对齐、检索口径确认、文献清单核验。
   这些过了之后,四部分连着一次性写完整篇,不再逐段停下来问用户。
4. **先写主体**:四部分里先写"文献梳理与分析",它定了全篇口径,之后回填背景、现状、参考文献才接得上。
   这只是内部撰写顺序,不是分批交付。

## 交互总则:所有干预点一律走 AskUserQuestion

这个 skill 的人工干预**必须用 AskUserQuestion 工具发起**,不要用普通文字提问代替。凡是需要用户拍板、确认、
选择的地方,都调 AskUserQuestion,把选项列出来让用户点。下面每个 Phase 标了 🟢**[AskUserQuestion]** 的地方,
都是强制调用点,不许跳过、不许改成"你觉得呢?"这类开放式文字问法。

干预点全部集中在动笔之前,一共这几处强制 AskUserQuestion:

1. Phase 0 开场四连问(题目、文献来源、输出格式、引用规范),按需追问期刊名和字数
2. Phase 1-C 检索口径确认(动手搜之前问清文献规模、年份跨度、来源门槛、语种配比)
3. Phase 1 文献清单核验(列出候选或抽取结果后,让用户确认/删改;走"已有材料"那条路时还要让用户裁决材料间的矛盾)
4. LaTeX 模板来源确认(找到模板后让用户确认用不用)
5. 系统综述配 PRISMA 图时,筛选数字并入 Phase 1 的清单核验一次问清,出图阶段不再问

**这些前期干预过了之后,Phase 2 撰写正文阶段不再调用 AskUserQuestion**:四部分连着一次性写完,
直接输出完整初稿,不逐段停下来问用户可不可行。

## 工作流程

### Phase 0:开场对齐 🟢[AskUserQuestion]

**调用一次 AskUserQuestion,把下面四个问题一起问**(题目那题用户多半会填"其他"自由输入):

- **论文题目/研究方向**:选项给"我在下方输入题目"之类,主要靠用户自填。
- **文献来源**:选项 = 「我已有现成材料(开题报告/初稿/参考文献列表),我发给你」/「我自己粘贴文献清单」/
  「让 AI 联网搜索文献」。
- **输出格式**:选项 = 「直接对话输出」/「Markdown 文件」/「Word」/「LaTeX」。
- **引用规范**:选项 = 「国标 GB/T 7714-2015(默认)」/「APA 第七版」/「目标期刊自有格式」/
  「学校模板里有要求,我发给你」。选了期刊自有格式的要问清期刊名。

第一批答完后按需补第二批 AskUserQuestion:选了 LaTeX 的追问目标期刊或会议名称(用于搜模板);
另外问一次**篇幅要求**,选项 = 「三千字左右」/「五千字左右」/「八千字以上」/「学校有硬性要求,我说明」。
信息不全绝不自己假设,缺哪问哪。

**用户选了「我已有现成材料」时,题目那题可以先放着**:材料里多半就写着题目和研究方向,
Read 完抽出来再跟用户确认,比让他手打一遍强。引用规范和字数同理,任务书和学校模板里常有,
抽到了就不用再问。走 Phase 1 的 A 路。

### Phase 1:备齐真文献

按 Phase 0 选的来源分三条路。

**A. 用户已有现成材料(先抽,再只问缺的)**

学生手里常有开题报告、导师给的初稿、任务书、从知网/Web of Science 导出的参考文献,
或者一份写了一半的综述。这条路的原则是:**能从材料里抽的就不要再问用户一遍。**

1. 让用户把文件发过来或给出路径,常见有开题报告、研究计划、任务书、初稿或半成品综述、
   参考文献导出文件(.txt/.ris/.bib/.docx/.xlsx)、导师批注版。
2. 用 Read 读进来(.docx 用 Bash 调 pandoc 或 python-docx 转文本;.pdf 用 Read;
   表格用 Read 或 pandas)。读不了的格式如实说,让用户粘文本。
3. 抽这几项:论文题目、研究方向与核心问题、已有文献清单、年份范围与学科限定、
   引用规范要求、字数要求、学校或期刊的格式要求。
4. **多份材料要交叉核对,把矛盾单独列出来问用户**。典型矛盾:开题报告写"近五年"但初稿里引了
   十年前的文献;任务书的题目和初稿标题不一致;参考文献列表里有正文没引用过的条目。
   不要自己挑一个默认,列给用户拍板。
5. 材料里的文献**仍然要过真实性核验**——用户自己整理的清单照用,但字段缺失(没年份、没期刊、
   没出处)的要标出来;明显是 AI 生成或来源不明的条目要单独问,不要因为"用户给的"就直接采信。
6. 按下方《证据等级》给每条标 A/B 级,统计一下 B 级占比,这决定正文能写多深。
7. 把抽取结果整理成一份摘要列给用户看(题目、方向、清单条目数、A/B 级分布、缺的字段、发现的矛盾),
   然后 🟢**[AskUserQuestion]** 让用户确认,选项 =
   「抽取的信息都对,用这份清单开始写」/「有几处要改(在备注里说明)」/「文献不够,再帮我搜一些补上」/
   「我把重点文献的摘要或全文再发给你」。用户选了补文献就接着走 C 路补齐,补完再合并核验一次。

材料里信息不全的部分,**只问缺的那几项**,不要把 Phase 0 四连问重新走一遍。

**B. 用户自己粘贴清单**

让用户把中英文文献清单贴进来。**明确提醒他连摘要一起贴**:导出时选信息最全的格式
(标题、作者、年份、刊名、摘要都带上)。摘要在不在,直接决定正文能不能写出这篇文献到底做了什么——
只有题录的条目按 B 级处理,正文里只能写到主题层面。粘完同样按《证据等级》标级,然后走清单核验。

**C. AI 联网搜索(先定检索口径,再防编造)**

**动手搜之前,先 🟢[AskUserQuestion] 把检索口径问清楚。** 文献要多少篇、圈几年、什么档次的
刊物能用、中英文各占多少,这些是学科惯例和导师要求决定的,不是通用默认值能替用户拍板的。一次问这四项:

- **文献规模**:选项 = 「15 篇左右(小节或课程作业)」/「25 篇左右(学位论文的研究现状)」/「40 篇以上(期刊综述)」/「按字数定,你帮我判断」。
- **年份跨度**:选项 = 「近五年」/「近十年」/「不限年份,重点看奠基性文献」/「近三年,领域变化快」。
- **来源门槛**:选项 = 「只要核心期刊(中文 CSSCI/北大核心,英文 SCI/SSCI)」/「核心为主,允许少量普刊和学位论文」/「顶会顶刊优先,可含 arXiv 预印本」/「不限,先求覆盖面」。
- **中英文配比**:选项 = 「中英各半」/「以中文为主」/「以英文为主」/「不限,按检索结果实际分布」。

口径定了再检索:

- 用 WebSearch 按题目关键词检索,中文题目搜中文库、英文题目搜英文库;年份、来源门槛按上面确认的口径卡。
- **一个主题至少换两三组同义词或近义表述再搜一轮**,别只用题目原词搜一遍就收工。
- **留一份检索记录**:检索词、检索到的站点或库、检索日期。这份记录要写进 Phase 2「研究背景与目的」的范围那一段,让读者知道范围是怎么划出来的。
- 每一篇候选文献都要能追到真实出处(期刊/数据库/DOI/可访问链接),必要时用 WebFetch 打开核对标题、作者、年份是否属实。**候选超过十五篇时用 Agent 并行核验**,一个子代理核一批,比串着挨个 Fetch 快得多。
- 追不到出处、或信息对不上的,一律丢弃,绝不写进清单充数。
- 顺手把摘要抓下来,抓到摘要的标 A 级、只有题录的标 B 级(见下方《证据等级》)。
- 逐条标注刊物层级(核心/普刊/学位论文/预印本/会议)。**预印本、来源不明的网站转载、疑似掠夺性期刊要单独拎出来提示用户**,不要跟核心期刊混在一起蒙过去。
- **如实说明检索的局限**:WebSearch 只能覆盖开放网络,进不了知网、万方、Web of Science、Scopus 的付费全文,结果会偏向开放获取文献。建议用户用学校数据库补几篇领域内公认的核心文献和奠基性文献,这是开放网络搜不全的部分。
- 🟢**[AskUserQuestion]** 先把核验过的候选清单(编号、标题、作者、年份、刊物层级、证据等级、出处链接)完整列在正文里给用户看,**然后调用 AskUserQuestion** 问用户是否采用,选项 = 「这份清单可以,开始写」/「我要删掉或替换某几篇(在备注里说明)」/「我再补几篇学校数据库里的核心文献」/「重新搜/我改用自己粘贴」。用户确认前不写正文。

**证据等级(三条路共用,决定正文能写多深)**

进 Phase 2 之前,清单里每一条都要带一个等级标记,这个标记直接管着正文的写法:

- **A 级**:拿到了摘要或全文。正文里可以写这篇文献用了什么方法、在什么数据上、得到什么结论,可以写具体的模型名和数值。
- **B 级**:只有题录(标题、作者、年份、刊名)。正文里**只能写到主题层面**,例如某某[3]从供应链协同角度切入这一问题。**不许出现原文没读到的方法名、数据集名、指标和百分比。**
- **C 级**:出处存疑或核不上。不写进正文,退回给用户确认或直接删掉。

**B 级占到一半以上时,交付说明里要如实讲清楚**:这篇综述目前只能写到主题层面,想让正文写出各篇文献的具体做法和结论,需要补摘要或全文。补不补是用户的决定,但不能瞒着他把正文写得像都读过一样。

无论哪条路,进入 Phase 2 前,手里都要有一份用户经 AskUserQuestion 认可、且逐条带等级标记的真实文献清单。

### Phase 2:一次性写完整篇正文(不再逐段确认)

前期对齐和文献清单都过了,这一阶段**连着把四部分一口气写完,不再调用 AskUserQuestion,不逐段停下来问用户**。

**动笔前先 Read `references/prompts.md`**,把四部分的提示词正文、《证据纪律》和《行文要求》一次性读进上下文再写——不要凭记忆写,这是防止长流程里遗忘和文风漂移的关键动作。
写参考文献那一部分之前,再 Read 一次 `references/citation-format.md`,按细则排格式,别凭记忆套国标。

内部撰写顺序仍按下面来(先写主体定口径),但这是你自己写作的顺序,不是分批交给用户:
**① 文献梳理与分析(主体,先写)→ ② 研究背景与目的 → ③ 研究现状总结与评述 → ④ 参考文献。**

四部分都写完后做三轮自查,都过了再交付:

1. **证据核查(最要紧的一轮)**:把正文里每一处具体表述——方法名、模型名、数据集、指标、数值、比例——挨个对回它所引文献的证据等级。A 级的核实原文确实这么写;B 级的一旦出现具体细节,删掉改成主题层面的说法。**宁可把正文写薄,也不留一句编出来的结论。** 参考文献列表全真、正文转述失实,比整条假引用更难被发现,危害更大。
2. 拿《行文要求》对照全文查文风(有没有冒出列表体、排比、"综上所述"、半角标点、中英文之间空格等);
3. **Read `references/completeness-checklist.md`**,逐项核内容完整性(文献用全了没、结论分歧写了没、空白指得具体不具体、范围表述和清单对不对得上)。

三轮里发现的问题自己先修好,然后**把完整的一篇初稿一次性输出/交付**(按 Phase 3 的格式)。

**可选配图**:文献超过十篇、且分了三个及以上主题分支时,值得给一张文献分类框架图。
要出图就 Read `references/diagrams.md`,**图一律调 Image2 模型生成**,提示词里显式点名 Image2。
图里的内容全部来自已定稿正文,不新增文献和结论;出图后逐框核对文字和编号,错了重出。
用户没提配图需求、或正文小标题已经足够清楚时,不必硬加。

### Phase 3:按格式交付完整初稿

把 Phase 2 写好的整篇按 Phase 0 选的格式一次性落地,并明确告诉用户"这是完整初稿,自己跑一遍看效果,哪里要改再一起改":

- **对话**:直接输出。
- **Markdown**:用 Write 写成 .md 文件。
- **Word**:见下方专门流程。
- **LaTeX**:见下方专门流程。

**交付说明里必须交代清楚**:这是初稿;哪几篇是 B 级证据、正文只写到了主题层面;检索的局限(开放网络搜不到付费库全文)和建议用户自己补的部分;可选项(配图、汇总表)哪些没做。

---

## reference 文件与读取时机

拆成独立文件是为了在动笔前用 Read 就近把内容重新灌进上下文,抗遗忘、抗上下文压缩。
不要凭记忆代替 Read。

| 文件 | 内容 | 什么时候 Read |
|---|---|---|
| `references/prompts.md` | 四部分提示词正文 +《证据使用规则》+《行文要求》 | Phase 2 动笔前(必读) |
| `references/citation-format.md` | GB/T 7714-2015 细则 | 写参考文献那部分之前(必读) |
| `references/completeness-checklist.md` | 证据核查 + 内容完整性清单 | 四部分写完、交付前(必读) |
| `references/diagrams.md` | Image2 配图提示词 | 决定出图时 |

---

## 工具用途

frontmatter 里声明的工具各有明确用途,别声明了不用,也别拿别的工具凑:

| 工具 | 用在哪 |
|---|---|
| Read | 读用户材料(A 路)、读四份 reference |
| Glob / Grep | 用户给的是目录或多份材料时定位文件、在长材料里找题目和参考文献段 |
| Write / Edit | 输出 .md / .tex,写中间稿 |
| Bash | .docx 转文本(pandoc / python-docx)、生成 .docx 并验证文件有效 |
| WebSearch / WebFetch | C 路检索文献、核对出处、找期刊 LaTeX 模板 |
| Agent | 候选文献超过十五篇时并行核验出处,一个子代理核一批 |
| AskUserQuestion | Phase 0 开场对齐、Phase 1 检索口径与清单核验、LaTeX 模板确认 |

---

## Word 输出流程

用户选 Word 时,先用 Markdown 把全文写定,再转 .docx。
优先 Bash 调 pandoc,没有 pandoc 就用 python-docx;两个都没有,**先交付 Markdown 并如实说明没法转 docx**,不要假装已经生成。

**格式规格**(用户或学校/期刊另有要求时以对方为准):

- 页面 A4,四边边距 2.54 厘米(1 英寸)
- 正文中文宋体、英文 Times New Roman,小四号(12pt)
- 行距 1.5 倍(学位论文常见要求);用户明确要双倍行距就用双倍
- 段首缩进两个中文字符,段间不额外加空行
- 标题:一级标题黑体小三加粗,二级标题黑体四号,层级用 Word 内置标题样式(Heading 1/2/3),别用手动加粗冒充,否则生成不了目录
- 页码居中或右对齐置于页脚
- 参考文献采用悬挂缩进,编号 `[1]` 顶格、续行与首行文字左对齐
- 表格:表题在表格**上方**,居中,格式"表 1 表题";图:图题在图**下方**,居中,格式"图 1 图题";表图都要在正文里被提到
- 三线表优先(上下框线加表头下横线),不要满格网格线

**生成后验证**:用 Bash 打开生成的 .docx 确认文件有效、不是零字节、能正常读出段落数(python-docx 读一遍或 pandoc 反向转一次都行)。验证不过就修,别把坏文件交出去。交付时说明这是初稿,让用户自己打开跑一遍看格式。

---

## LaTeX 输出流程

用户选 LaTeX 时:

1. **搜真实期刊模板**:用 WebSearch/WebFetch 找目标期刊或会议的官方 LaTeX 模板(官网、Overleaf 官方模板库、出版社页面)。
2. **给用户核验** 🟢**[AskUserQuestion]**:把找到的模板来源(名称+链接)列出来,调用 AskUserQuestion 让用户确认,选项 = 「用这个官方模板」/「都不对,用通用模板」/「我另外提供模板链接」。和搜文献一个逻辑——不确定、疑似非官方/山寨的不用。
3. **降级兜底**:确实找不到官方模板,就用通用 `article` 类(带 ctex 支持中文)套,并**明确告诉用户"没找到该期刊官方模板,当前用的是通用模板"**,不要伪造成官方模板。
4. **始终产出可编译的 .tex**:把正文和参考文献填进模板,保证结构完整能编译。
5. 交付时说明这是初稿,让用户自己套进期刊环境跑一遍看效果。

---

## 执行检查清单

交付前逐项过:

- [ ] Phase 0 用 AskUserQuestion 问全了题目、文献来源、输出格式、引用规范(LaTeX 还问了期刊,篇幅要求已问或已从材料里抽到)
- [ ] 用户有现成材料的:读完先抽了信息、只问了缺的那几项,没把开场问答重走一遍;材料间的矛盾已列给用户裁决
- [ ] 走 C 路的:动手搜之前已用 AskUserQuestion 确认过文献规模、年份跨度、来源门槛、语种配比,没有自己拍默认值
- [ ] 走 C 路的:检索记录(检索词、库/站点、检索日期)已留存,并写进了"研究背景与目的"的范围那一段
- [ ] 写入正文的文献全部真实、有出处;清单已用 AskUserQuestion 让用户核验;每条都标了刊物层级和证据等级
- [ ] **正文里的方法名、数据集、指标、数值全部来自 A 级证据的原文;B 级文献只写到主题层面,没有编出来的结论**
- [ ] 前期对齐和文献核验都过了之后,四部分**一次性写完整篇**,撰写阶段没再逐段调 AskUserQuestion
- [ ] 内部撰写顺序先写了「文献梳理与分析」主体
- [ ] 正文全部文献都用上、按序号标注、点了作者名
- [ ] 评述部分写到了结论分歧、方法学局限、空白的具体落点和它为什么还在,未来方向和空白一一对应
- [ ] 动笔前 Read 了 `references/prompts.md`,整篇写完套《行文要求》自查过,AI 腔已压低
- [ ] 写参考文献前 Read 了 `references/citation-format.md`,格式按细则核过(默认国标 GB/T 7714-2015,条目内用半角标点加空格)
- [ ] 交付前 Read 了 `references/completeness-checklist.md` 并逐项核过证据与内容完整性
- [ ] 出图的话:Read 了 `references/diagrams.md`,提示词显式点名 Image2,出图后逐框核对过文字和编号;PRISMA 图的数字全部来自用户已确认的,没自己推算补齐
- [ ] Word 输出:按格式规格生成,生成后验证过文件有效
- [ ] LaTeX:模板来源用 AskUserQuestion 让用户确认,找不到官方已降级并如实告知
- [ ] 交付说明讲明了这是初稿、哪几篇是 B 级证据、检索局限、可选项哪些没做
