---
name: literature-review
description: 中文学术文献综述全流程生成。分四部分(研究背景与目的、文献梳理与分析、研究现状总结与评述、参考文献),开场先对齐题目、文献来源、输出格式,文献经核验后一次性写完整篇再输出;文献可用户已有材料抽取、自己粘贴或 AI 联网搜索,搜索结果必须带真实出处、经核验才写入正文,严防编造引用;参考文献默认国标 GB/T 7714-2015;支持对话/Markdown/Word/LaTeX 输出,可选调用 Image2 生成文献分类框架图。本 skill 面向叙述性文献综述,不做元分析和统计合并,不做 PRISMA 全流程系统综述的筛选与偏倚评价。触发:文献综述、写综述、literature review、帮我写文献综述、国内外研究现状。
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Agent
  - AskUserQuestion
  - WebSearch
  - WebFetch
---

# 文献综述生成 Skill

按四部分推进一篇文献综述:研究背景与目的 → 文献梳理与分析 → 研究现状总结与评述 → 参考文献。
核心是**前期对齐充分、文献真实可核验**:开场把题目、文献来源、输出格式一次性问清,文献清单经用户核验,
然后一口气写完整篇初稿再交付,不再一段段停下来确认。

## 触发条件

- 用户要写文献综述 / 国内外研究现状
- 关键词:文献综述、写综述、literature review、帮我写文献综述、国内外研究现状

## 三条铁律

1. **文献必须真**:写入正文的每一篇文献都要有可核验来源。用户自己粘的照用;AI 搜的必须带出处链接、先给用户核对,禁止凭记忆编造标题、作者、年份。
2. **前期对齐、一次成稿**:干预集中在动笔之前——开场对齐、文献清单核验。这些过了之后,四部分连着一次性写完整篇,不再逐段停下来问用户。
3. **先写主体**:四部分里先写"文献梳理与分析",它定了全篇口径,之后回填背景、现状、参考文献才接得上。这只是内部撰写顺序,不是分批交付。

## 交互总则:所有干预点一律走 AskUserQuestion

这个 skill 的人工干预**必须用 AskUserQuestion 工具发起**,不要用普通文字提问代替。凡是需要用户拍板、确认、选择的地方,都调 AskUserQuestion,把选项列出来让用户点。下面每个 Phase 标了 🟢**[AskUserQuestion]** 的地方,都是强制调用点,不许跳过、不许改成"你觉得呢?"这类开放式文字问法。

干预点全部集中在动笔之前,一共这几处强制 AskUserQuestion:
1. Phase 0 开场三连问(题目、文献来源、输出格式)
2. Phase 1 文献清单核验(列出候选或抽取结果后,让用户确认/删改;走"已有材料"那条路时还要让用户裁决材料间的矛盾)
3. LaTeX 模板来源确认(找到模板后让用户确认用不用)

**这些前期干预过了之后,Phase 2 撰写正文阶段不再调用 AskUserQuestion**:四部分连着一次性写完,直接输出完整初稿,不逐段停下来问用户可不可行。

## 工作流程

### Phase 0:开场对齐 🟢[AskUserQuestion]

**调用一次 AskUserQuestion,把下面三个问题一起问**(题目那题用户多半会填"其他"自由输入):

- **论文题目/研究方向**:选项给"我在下方输入题目"之类,主要靠用户自填。
- **文献来源**:选项 = 「我已有现成材料(开题报告/初稿/参考文献列表),我发给你」/「我自己粘贴文献清单」/「让 AI 联网搜索文献」。
- **输出格式**:选项 = 「直接对话输出」/「Markdown 文件」/「Word」/「LaTeX」。

若用户选了 LaTeX,**再补一次 AskUserQuestion 或在同批问题里追问目标期刊/会议名称**(用于 LaTeX 流程搜模板)。信息不全绝不自己假设,缺哪问哪。

**用户选了「我已有现成材料」时,题目那题可以先放着**:材料里多半就写着题目和研究方向,
Read 完抽出来再跟用户确认,比让他手打一遍强。走 Phase 1 的 A 路。

### Phase 1:备齐真文献

按 Phase 0 选的来源分三条路。

**A. 用户已有现成材料(先抽,再只问缺的)**

学生手里常有开题报告、导师给的初稿、任务书、从知网/Web of Science 导出的参考文献,
或者一份写了一半的综述。这条路的原则是:**能从材料里抽的就不要再问用户一遍。**

1. 让用户把文件发过来或给出路径,常见有开题报告、研究计划、任务书、初稿或半成品综述、
   参考文献导出文件(.txt/.ris/.bib/.docx/.xlsx)、导师批注版。
2. 用 Read 读进来(.docx 用 Bash 调 pandoc 或 python-docx 转文本;.pdf 用 Read;
   表格用 Read 或 pandas)。读不了的格式如实说,让用户粘文本。
3. 抽这几项:论文题目、研究方向与核心问题、已有文献清单、年份范围与学科限定、
   引用规范要求、字数要求、学校或期刊的格式要求。
4. **多份材料要交叉核对,把矛盾单独列出来问用户**。典型矛盾:开题报告写"近五年"但初稿里引了
   十年前的文献;任务书的题目和初稿标题不一致;参考文献列表里有正文没引用过的条目。
   不要自己挑一个默认,列给用户拍板。
5. 材料里的文献**仍然要过真实性核验**——用户自己整理的清单照用,但字段缺失(没年份、没期刊、
   没出处)的要标出来;明显是 AI 生成或来源不明的条目要单独问,不要因为"用户给的"就直接采信。
6. 把抽取结果整理成一份摘要列给用户看(题目、方向、清单条目数、缺的字段、发现的矛盾),
   然后 🟢**[AskUserQuestion]** 让用户确认,选项 =
   「抽取的信息都对,用这份清单开始写」/「有几处要改(在备注里说明)」/「文献不够,再帮我搜一些补上」。
   用户选了补文献就接着走 C 路补齐,补完再合并核验一次。

材料里信息不全的部分,**只问缺的那几项**,不要把 Phase 0 三连问重新走一遍。

**B. 用户自己粘贴清单**
让用户把中英文文献清单贴进来。提醒他导出时选信息最全的格式(标题、作者、年份、摘要都带上),
字段越全,后面正文越不容易出错。

**C. AI 联网搜索(防编造是重点)**
- 用 WebSearch 按题目关键词检索,中文题目搜中文、英文题目搜英文;时间圈定近五年。
- 每一篇候选文献都要能追到真实出处(期刊/数据库/DOI/可访问链接),必要时用 WebFetch 打开核对标题、作者、年份是否属实。
- 追不到出处、或信息对不上的,一律丢弃,绝不写进清单充数。
- 数量凑够一批(二十篇上下、中英各半比较够用,依题目和用户要求定)。
- 🟢**[AskUserQuestion]** 先把核验过的候选清单(编号、标题、作者、年份、出处链接)完整列在正文里给用户看,**然后调用 AskUserQuestion** 问用户是否采用这份清单,选项 = 「这份清单可以,开始写」/「我要删掉或替换某几篇(在备注里说明)」/「重新搜/我改用自己粘贴」。用户确认前不写正文。

无论哪条路,进入 Phase 2 前,手里都要有一份用户经 AskUserQuestion 认可的真实文献清单。

### Phase 2:一次性写完整篇正文(不再逐段确认)

前期对齐和文献清单都过了,这一阶段**连着把四部分一口气写完,不再调用 AskUserQuestion,不逐段停下来问用户**。

**动笔前先 Read `references/prompts.md`**,把四部分的提示词正文和《行文要求》一次性读进上下文再写——不要凭记忆写,这是防止长流程里遗忘和文风漂移的关键动作。
写参考文献那一部分之前,再 Read 一次 `references/citation-format.md`,按细则排格式,别凭记忆套国标。

内部撰写顺序仍按下面来(先写主体定口径),但这是你自己写作的顺序,不是分批交给用户:
**① 文献梳理与分析(主体,先写)→ ② 研究背景与目的 → ③ 研究现状总结与评述 → ④ 参考文献。**

四部分都写完后做两轮自查,都过了再交付:

1. 拿《行文要求》对照全文查文风(有没有冒出列表体、排比、"综上所述"、半角标点、中英文之间空格等);
2. **Read `references/completeness-checklist.md`**,逐项核内容完整性(文献用全了没、空白指得具体不具体、范围表述和清单对不对得上)。

两轮里发现的问题自己先修好,然后**把完整的一篇初稿一次性输出/交付**(按 Phase 3 的格式)。

**可选配图**:文献超过十篇、且分了三个及以上主题分支时,值得给一张文献分类框架图。
要出图就 Read `references/diagrams.md`,**图一律调 Image2 模型生成**,提示词里显式点名 Image2。
图里的内容全部来自已定稿正文,不新增文献和结论;出图后逐框核对文字和编号,错了重出。
用户没提配图需求、或正文小标题已经足够清楚时,不必硬加。

### Phase 3:按格式交付完整初稿

把 Phase 2 写好的整篇按 Phase 0 选的格式一次性落地,并明确告诉用户"这是完整初稿,自己跑一遍看效果,哪里要改再一起改":

- **对话**:直接输出。
- **Markdown**:用 Write 写成 .md 文件。
- **Word**:见下方专门流程。
- **LaTeX**:见下方专门流程。

---

## reference 文件与读取时机

拆成独立文件是为了在动笔前用 Read 就近把内容重新灌进上下文,抗遗忘、抗上下文压缩。
不要凭记忆代替 Read。

| 文件 | 内容 | 什么时候 Read |
|---|---|---|
| `references/prompts.md` | 四部分提示词正文 + 《行文要求》 | Phase 2 动笔前(必读) |
| `references/citation-format.md` | GB/T 7714-2015 细则 | 写参考文献那部分之前(必读) |
| `references/completeness-checklist.md` | 内容完整性清单 | 四部分写完、交付前(必读) |
| `references/diagrams.md` | Image2 配图提示词 | 决定出图时 |

---

## Word 输出流程

用户选 Word 时,先用 Markdown 把全文写定,再转 .docx。
优先 Bash 调 pandoc,没有 pandoc 就用 python-docx;两个都没有,**先交付 Markdown 并如实说明没法转 docx**,不要假装已经生成。

**格式规格**(用户或学校/期刊另有要求时以对方为准):

- 页面 A4,四边边距 2.54 厘米(1 英寸)
- 正文中文宋体、英文 Times New Roman,小四号(12pt)
- 行距 1.5 倍(学位论文常见要求);用户明确要双倍行距就用双倍
- 段首缩进两个中文字符,段间不额外加空行
- 标题:一级标题黑体小三加粗,二级标题黑体四号,层级用 Word 内置标题样式(Heading 1/2/3),别用手动加粗冒充,否则生成不了目录
- 页码居中或右对齐置于页脚
- 参考文献采用悬挂缩进,编号 `[1]` 顶格、续行与首行文字左对齐
- 表格:表题在表格**上方**,居中,格式"表 1 表题";图:图题在图**下方**,居中,格式"图 1 图题";表图都要在正文里被提到
- 三线表优先(上下框线加表头下横线),不要满格网格线

**生成后验证**:用 Bash 打开生成的 .docx 确认文件有效、不是零字节、能正常读出段落数(python-docx 读一遍或 pandoc 反向转一次都行)。验证不过就修,别把坏文件交出去。交付时说明这是初稿,让用户自己打开跑一遍看格式。

---

## LaTeX 输出流程

用户选 LaTeX 时:

1. **搜真实期刊模板**:用 WebSearch/WebFetch 找目标期刊或会议的官方 LaTeX 模板(官网、Overleaf 官方模板库、出版社页面)。
2. **给用户核验** 🟢**[AskUserQuestion]**:把找到的模板来源(名称+链接)列出来,调用 AskUserQuestion 让用户确认,选项 = 「用这个官方模板」/「都不对,用通用模板」/「我另外提供模板链接」。和搜文献一个逻辑——不确定、疑似非官方/山寨的不用。
3. **降级兜底**:确实找不到官方模板,就用通用 `article` 类(带 ctex 支持中文)套,并**明确告诉用户"没找到该期刊官方模板,当前用的是通用模板"**,不要伪造成官方模板。
4. **始终产出可编译的 .tex**:把正文和参考文献填进模板,保证结构完整能编译。
5. 交付时说明这是初稿,让用户自己套进期刊环境跑一遍看效果。

---

## 执行检查清单

交付前逐项过:

- [ ] Phase 0 用 AskUserQuestion 问全了题目、文献来源、输出格式(LaTeX 还问了期刊)
- [ ] 用户有现成材料的:读完先抽了信息、只问了缺的那几项,没把三连问重走一遍;材料间的矛盾已列给用户裁决
- [ ] 写入正文的文献全部真实、有出处;AI 搜的已用 AskUserQuestion 让用户核验清单
- [ ] 前期对齐和文献核验都过了之后,四部分**一次性写完整篇**,撰写阶段没再逐段调 AskUserQuestion
- [ ] 内部撰写顺序先写了「文献梳理与分析」主体
- [ ] 正文全部文献都用上、按序号标注、点了作者名
- [ ] 动笔前 Read 了 `references/prompts.md`,整篇写完套《行文要求》自查过,AI 腔已压低
- [ ] 写参考文献前 Read 了 `references/citation-format.md`,格式按细则核过(默认国标 GB/T 7714-2015)
- [ ] 交付前 Read 了 `references/completeness-checklist.md` 并逐项核过内容完整性
- [ ] 出图的话:Read 了 `references/diagrams.md`,提示词显式点名 Image2,出图后逐框核对过文字和编号
- [ ] Word 输出:按格式规格生成,生成后验证过文件有效
- [ ] LaTeX:模板来源用 AskUserQuestion 让用户确认,找不到官方已降级并如实告知
- [ ] 交付时说明这是初稿,提示用户自己跑一遍再一起改
